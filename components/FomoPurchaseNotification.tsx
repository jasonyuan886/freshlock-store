'use client';

// FOMO purchase notifications disabled — previous version used fabricated
// purchase data ("Someone in US just bought..."), which is deceptive
// social proof. Re-enable only when connected to real order data.
export default function FomoPurchaseNotification() {
  return null;
}
